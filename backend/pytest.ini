[pytest]
testpaths = tests
pythonpath = . tests
addopts = -q --tb=short --strict-markers -p no:cacheprovider
markers =
    lent: test nécessitant davantage de temps (non exécuté par défaut)
filterwarnings =
    ignore::DeprecationWarning:starlette.*
    ignore::DeprecationWarning:fastapi.*
